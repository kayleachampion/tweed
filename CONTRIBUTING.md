# Tweed Development Principles

- Prefer simple text formats over complex abstractions.
- Never overwrite handwritten content without explicit markers.
- Every command should support --dry-run where practical.
- Favor composition over inheritance.
- If a feature requires Obsidian specifically, ask whether it can instead target plain Markdown.
- The vault is the source of truth; Tweed should never require a separate database.
